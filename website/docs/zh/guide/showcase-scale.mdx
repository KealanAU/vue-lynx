# Showcase 缩放对比

内部检查：`fit` @ 360×804 的 live web，密度是否对齐最早的首页 showcase
录屏（1080×2412 = 360×804 CSS 像素 @3×）？

左侧 = 录屏（`object-fit: cover`）。右侧 = 同一手机框里的
`<Go mode="preview" webPreviewMode="fit">`。字号 / 卡片尺寸应对齐；若 live
侧明显偏大，说明设计画布太小（或退回成了按框 CSS 像素做的 responsive 布局）。

<ScaleCompare />
